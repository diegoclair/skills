name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: lybel-docs/cli/go.mod
          cache: true
          cache-dependency-path: lybel-docs/cli/go.sum

      - name: Run tests
        working-directory: lybel-docs/cli
        run: make test

      - name: Cross-compile binaries
        working-directory: lybel-docs/cli
        run: make build-all VERSION=${{ github.ref_name }}

      - name: Package platform ZIPs
        run: |
          set -euo pipefail
          mkdir -p release-assets

          # platform : source binary name in dist/ : destination name in ZIP
          entries=(
            "darwin-amd64:lybel-docs-darwin-amd64:lybel-docs"
            "darwin-arm64:lybel-docs-darwin-arm64:lybel-docs"
            "linux-amd64:lybel-docs-linux-amd64:lybel-docs"
            "linux-arm64:lybel-docs-linux-arm64:lybel-docs"
            "windows-amd64:lybel-docs-windows-amd64.exe:lybel-docs.exe"
          )

          for entry in "${entries[@]}"; do
            IFS=':' read -r platform src_bin dst_bin <<< "$entry"
            stage="$(mktemp -d)"

            cp lybel-docs/SKILL.md "$stage/SKILL.md"
            cp -r lybel-docs/reference "$stage/reference"

            mkdir -p "$stage/bin"
            cp "lybel-docs/cli/dist/$src_bin" "$stage/bin/$dst_bin"
            chmod +x "$stage/bin/$dst_bin"

            (cd "$stage" && zip -r "$GITHUB_WORKSPACE/release-assets/lybel-docs-$platform.zip" . -q)
            rm -rf "$stage"
          done

          echo "--- release assets ---"
          ls -lh release-assets/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          files: release-assets/*.zip
          generate_release_notes: true
          fail_on_unmatched_files: true
          make_latest: true
